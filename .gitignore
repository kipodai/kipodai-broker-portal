# Real supplier data — must never be committed (see CLAUDE.md)
fixtures/

# Dependencies
node_modules/

# Build output
dist/
.netlify/
.wrangler/

# Env / secrets
.env
.env.local
.dev.vars

# Editor / OS
.DS_Store
Thumbs.db

# Claude Code session/runtime state (not project source)
.claude/

# Written into this folder by a different tool (Codex) working on unrelated
# projects on this machine — not part of this project, never commit it.
AGENTS.md

# Logs
*.log
npm-debug.log*
