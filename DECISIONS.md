# Decisions Log

Ambiguous product/implementation choices, and why. Newest at bottom.

## Public landing-page refinement (2026-08-21)

- **The marketing-site “Client portal” link now leads to a neutral `/client-portal` entry page, not directly to the existing Mamma Chia report portal.** The report portal remains at `/portal` and the post-login destination stays unchanged, so current client access is preserved while the public-facing navigation is ready for multiple future client dashboards.
- **The understated text-only industry strip was replaced by a visual “data sources → clear decisions” flow.** The original strip was easily missed in normal scrolling; the replacement communicates the same breadth (sales, inventory, operations) but earns its place as a distinct visual transition between the hero and services.
- **The landing page uses anonymized dashboard images instead of real client reporting screenshots.** Their chart/map structure demonstrates the combined supplier and retailer perspective without exposing customer, product, item, retailer, or business-specific details. They are marketing illustrations only, not live data.
- **The showcase headline now addresses suppliers directly:** “See your plan through your customers’ reality.” This keeps the distinction clear: the supplier owns the plan; their customers’ retail experience validates, challenges, or informs it.

## Phase 0 — Scaffold

- **Package manager / bundler:** Vite + React, `react-router-dom` for the 4 routes (`/login`, `/`, `/archive`, `/admin/upload`, `/report/:week`). Spec didn't mandate a router; chosen because it's the standard fit for a small multi-page SPA and keeps route/role checks readable.
- **API path convention:** frontend calls `/api/*`, redirected via `netlify.toml` to `/.netlify/functions/*`. Keeps frontend fetch calls stable if the functions runtime ever moves (e.g. Cloudflare fallback).
- **Anthropic / Resend calls via raw `fetch`, not SDKs:** both APIs are simple enough over HTTP that adding `@anthropic-ai/sdk` and `resend` as dependencies wasn't worth it. Netlify Functions run on Node 18+, which has global `fetch`. Reduces dependency surface and makes the "optional key → fallback" branching easy to read in one file.
- **Session auth via HMAC-signed cookie, no library:** Node's built-in `crypto` module covers this; spec explicitly asked to keep auth lightweight and not build OAuth.
- **Shared pure logic lives in `server/`, not `netlify/functions/`:** `server/parser.js` and `server/metrics.js` are pure modules with no Netlify runtime dependency, so Vitest can import and test them directly against `/fixtures` without spinning up functions. `netlify/functions/*.js` are thin HTTP wrappers that call into `server/`.

## Phase 1 — Parser

- **Real fixture filenames don't match the spec's exact substrings.** The actual ABI Studio exports in `/fixtures` are named `Mamma Chia_Weekly Trends_...`, `Mamma Chia_Trend Analysis_...`, `Mamma Chia_Geo Performance By State Data Selection Table_...` — spaces, not underscores. The spec anticipated this ("fall back to header-row detection if names change"), so `classifyByFilename` tries the exact substring first and `classifyByHeaderContent` (anchor-cell match on `WM Week` / `Week` / `State`) is the real-world path for all three sample files. This is exercised directly in `tests/parser.test.js` ("filename drift falls back to header detection").
- **Header row search is bounded to the first 5 rows.** Prevents a stray data cell that happens to equal an anchor string from being misread as a header row, while still tolerating the known 0-row and 1-row preambles.
- **All non-week/non-State columns coerced to number-or-null**, never left as raw strings — this is what makes "blank LY → null, never 0" hold uniformly across File A/B/C without per-column special-casing.
- **Geo_Performance parses by header name for every column except `State`**, since the spec says the column set "may include more" — only `State` is a hard requirement; anything else present is carried through as a coerced number.

## Phase 2 — Metrics engine

- **All vs-LY percentages are recomputed from raw TY/LY dollar figures**, not read from ABI Studio's own pre-computed `%Chg vs LY` columns. Keeps L1W and L4W (summed) comparisons using the identical formula (`(ty-ly)/ly`) instead of mixing a per-row provided value for L1W with a self-computed one for L4W.
- **U/S/W momentum uses `U/S/W (Valid Store)`**, not the `(POS Stores)` variant — chosen for consistency with the store-count gap metric, which is explicitly framed as Valid Store Count vs POS Store Count.
- **"Momentum negative two periods running"** is implemented as: L4W-vs-P4W negative AND P4W-vs-(P4W-prior) negative — i.e. two consecutive period-over-period comparisons, requiring 12 weeks of history. With fewer than 12 weeks, the flag simply can't fire (treated as `null`/false, not an error).
- **Supply-driven correlation thresholds:** instock < 95% (the existing flag threshold) and POS $ decline ≤ -15% vs LY, both within the most recent 13 weeks, with a non-empty week-overlap between the two sets. -15% sits between the -10% plain decline-flag threshold and the sample's actual -22% to -34% dip, specifically so a POS-only decline (no instock drop) doesn't accidentally satisfy the correlation on its own — verified by both a real-fixture test and a synthetic false-positive test.
- **OTIF alerting beyond the literal Alert Rules list:** the spec's Alert Rules section only names "OTIF In Full % < 95% (flag)", but the Metrics Engine section separately describes "flag either < 95% in L1W or a ≥3-point drop vs prior week" for File B generally. Implemented as: `otif_in_full_flag` (flag, In Full only, matching the canonical list) plus two additional `watch`-severity alerts for a ≥3-point single-week drop in either On Time % or In Full %. No standalone "On Time % < 95%" flag exists since it isn't in the canonical Alert Rules list.
- **Geography $500 floor applies to both growth and decline rankings**, applied identically — a state can't appear in "fastest growing" or "declining" without first clearing the floor, but the Top 5 by raw POS $ ranking ignores the floor entirely (a top-5 state is definitionally well above it in practice).

## Phase 4 — Auth / Storage / Functions

- **Netlify Functions v2 (Web `Request`/`Response`) used throughout**, not the classic `(event, context)` handler shape — chosen so `upload.js` can use `req.formData()` natively for the 3-file multipart upload instead of manually decoding a base64 body.
- **`server/auth.js` takes a raw cookie-header string, not the framework's request object**, decoupling session logic from the Netlify runtime shape entirely — this is what makes `tests/auth.test.js` able to unit-test cookie verification and `requireRole()` without any HTTP mocking.
- **Password comparison uses `crypto.timingSafeEqual`** via `safeStringEqual()`, not `===`, since two long-lived shared passwords are exactly the kind of low-traffic, high-value secret where a timing side-channel is worth closing for near-zero cost.
- **`Secure` cookie attribute is conditional on host** (`isLocalHost` check) rather than always-on, so local `netlify dev` over plain HTTP on `localhost` still receives the session cookie in the browser.
- **Upload flow is stateless preview→publish**, not a two-step "upload then confirm from server memory": the frontend resubmits the same 3 files with `mode=publish` after the admin reviews the `mode=preview` response. Simpler than holding parsed-but-unpublished state server-side, at the cost of re-parsing the files once more on publish — an acceptable trade for weekly-cadence file sizes.
- **Re-publishing a week preserves `sendLog`** (carried forward from the existing stored report, if any) — overwriting a report's numbers on a corrected re-upload should not erase the record of who it was already emailed to.
- **`/api/session` is a deliberately unauthenticated-safe, role-revealing endpoint** used only to drive frontend UI (which nav links/buttons to show). It is explicitly not a trust boundary — every admin-only function independently calls `requireRole()` regardless of what this endpoint reports, so a compromised or bypassed frontend can't grant itself admin UI access to real actions.

## Phase 5 — Narrative / Email

- **Anthropic and Resend called via raw `fetch`**, no SDK dependency (see Phase 0 decision above) — `generateNarrative()` catches any failure (network, non-2xx, empty text) and falls back to the deterministic template rather than propagating the error, so a flaky/misconfigured key degrades gracefully instead of blocking report generation.
- **Narrative template mirrors the Anthropic prompt's rules** (lead with the POS $ vs LY fact, explicit supply-driven callout when that alert key is present, otherwise fall through to other flag messages) so the two code paths produce comparably-shaped summaries even though only one calls out to Claude.
- **Email chart is a hand-built inline SVG polyline**, not a rendered chart image — avoids a server-side headless-browser/canvas dependency entirely; only POS $ TY vs LY is drawn (not Instock %, unlike the portal's dual-axis chart) to keep the email digest simple, per spec ("Keep the email to: exec summary, KPI table, alerts, link").
- **`send-email.js` re-validates recipients against `CLIENT_EMAIL_RECIPIENTS` server-side**, rejecting any address not on that list even if a direct API call bypasses the frontend's checkbox-only UI — this is the same "must never allow free-text recipients, ever, in any future feature" rule from `CLAUDE.md`, enforced at the one place that actually sends mail.

## Post-Phase-2 review findings (fixed)

An adversarial review subagent (see REVIEW LOOP) surfaced two real issues, both fixed:

- **Grand Total matching was case-sensitive** (`server/parser.js`, `parseGeoPerformance`): `row.State.startsWith('Grand Total')` would let a differently-cased export (`"grand total (us)"`) leak into state rankings uncaught. Fixed to `state.toLowerCase().startsWith('grand total')`. Regression test added in `tests/parser.test.js`.
- **The correlation-alert test hardcoded the spec's illustrative week range (202610–202612) as if it were guaranteed to be the active trigger.** It happened to still pass, but for a different reason than it claimed: the real fixture's `currentWeek` is 202628, so the correlation engine's trailing-13-week window is 202616–202628, and the actual overlap that fires the alert is weeks 202616–202619 — a separate, more recent supply dip. The 202610–202612 event described in the spec is real (verified independently) but by the time this fixture's "current week" reached 202628, that dip was 16+ weeks in the past and outside the window on purpose (a resolved issue from months ago shouldn't drive this week's supply-vs-demand callout). Since `/fixtures` gets regenerated weekly per `WEEKLY_RUNBOOK.md` and "current week" always advances, **any test that hardcodes specific week numbers against live fixtures will eventually go stale.** Fixed by splitting into two tests: one pins the historical 202610–202612 data-shape facts (pure regression check, independent of alert logic), the other computes the *expected* trailing-13-week overlap directly from the parsed data using the same threshold logic as `server/metrics.js`, then asserts the alert's `weeks` field matches — no hardcoded week numbers. The `supply_driven_correlation` alert object now also exposes a `weeks` array (previously the overlap was computed internally but never surfaced), which is what made this fix possible and may also be useful for the report UI later (e.g. highlighting the specific weeks on the chart).
- **Minor scope-gap additions**, also from the same review: added an `otif_on_time_watch` alert (On Time % < 95%, watch severity) mirroring the existing In Full % flag — the spec's Metrics Engine section describes both metrics symmetrically even though the canonical Alert Rules list only names In Full explicitly. Added `replInstockPct` to `instock.series13w` alongside `instockPct`, since the spec's Instock bullet names both metrics together.

## Post-launch feature: admin-editable email recipient list

- **`server/recipients.js` is the single shared resolver** (`getEffectiveRecipients()`) for "what addresses is the admin allowed to send to" — used by both `netlify/functions/email-config.js` (what the Send dialog offers) and `netlify/functions/send-email.js` (what it actually accepts on send). Having two independent implementations of this check was the exact failure mode to avoid, since a future edit to one and not the other would silently reopen the "no free-text recipients" hole `CLAUDE.md` explicitly protects.
- **Resolution order: Blobs-stored list wins once one has ever been saved; `CLIENT_EMAIL_RECIPIENTS` env var is only the initial seed.** There's no "revert to env var" button — once an admin saves any edit via `/admin/settings`, the env var is permanently superseded for that deployment. This matches the spec note in `CLAUDE.md` ("if the list moves from env var to Blobs-backed store...").
- **Emails are normalized (trimmed, lowercased, deduped) at write time**, not at read/validation time — `normalizeRecipients()` runs once in the PUT handler before saving. This means the stored list, what `email-config.js` returns to the UI, and what `send-email.js` validates against are always already-normalized strings; the frontend never needs to normalize anything itself, and a case-varied or padded duplicate can't sneak into the stored list.
- **`/admin/settings` is a new admin-only route/page**, gated the same way as `/admin/upload` (client-side `ProtectedRoute adminOnly` redirect + independent server-side `requireRole` check in `recipients.js` — the redirect is convenience only, never the actual boundary).

## Post-launch feature: item-level slicer (Sales_Performance, 4th optional file)

- **The item file's period set is discovered dynamically, never hardcoded.** The feature was scoped assuming LW/L4/L13/L25/L52; the real export turned out to use `LWk`/`L4Wk`/`L13Wk`/`L26Wk`/`L52Wk`/`YTD` (L26 not L25, plus a 6th period, YTD, added after the initial ask). `parseItemPerformance()` in `server/parser.js` reads whatever period labels are actually present in row 2 of the compound header and returns them as a `periods` array — the UI's period `<select>` is built from that array, not a fixed list. If ABI Studio adds/removes a period again, nothing breaks.
- **Two-row compound header (metric name × period) required new parsing logic**, not reuse of the existing single-header-row helpers (`findHeaderRowIndex` + `rowsFromMatrix`). SheetJS flattens a merged cell to a value in only its top-left cell and `''` for the rest of the merge, so the metric-name row needs forward-filling to know which metric a given column belongs to before pairing it with the period row directly below. This is genuinely new code (`parseItemPerformance`), not an extension of `rowsFromMatrix`.
- **The 4th file is optional at the parser and function level**, not just the UI: `parseUploadedFiles()` accepts exactly 3 or exactly 4 files; a 5-file (or 2-file) upload is rejected the same as before. `computeMetrics()` sets `itemPerformance: null` when the 4th file wasn't part of that week's upload, and `ItemPerformancePanel.jsx` renders a plain "Not included in this week's upload" message rather than crashing or hiding the whole section — matches the existing "app must fully function with pieces missing" ethos (optional API keys) applied to an optional input file instead.
- **`isGrandTotalLabel()` was extracted as a shared helper** (previously inlined in `parseGeoPerformance` only) since the item file's Grand Total row (`itemNbr = "Grand Total (30 Items)"`) needed the exact same case-insensitive-prefix exclusion logic. Both `parseGeoPerformance` and `parseItemPerformance` now call the same function — a real "State" or "Prime Item Nbr" field named `"grand total ..."` in any casing is excluded from the list and captured as the period/brand total either way.
- **Display metrics are a curated 6-item subset** (`DISPLAY_METRICS` in `ItemPerformancePanel.jsx`) of the ~19 metric groups the file actually carries, chosen to mirror the brand-level KPI cards (sales, growth vs LY, units, in-stock, units/store/week, % stores selling) rather than dumping all 19 into a wall of columns. Not spec-mandated (this whole feature is post-launch, user-directed) — a reasonable default judgment call, easy to expand later since the parser already captures every metric group, not just the curated six.
- **Default item selection is "top 5 by POS $ for the initially-selected period,"** computed once on mount, matching the existing Geography panel's "top 5 states" convention — so the table isn't empty on first render, but the user's subsequent checkbox choices are never overridden by a period change (recomputing the top-5 default every time the period dropdown changes would be surprising/annoying).
- **Verification gap, disclosed rather than hidden:** this feature's backend is verified thoroughly — 21 parser tests (11 new: real fixture item count/periods/blanks/Grand-Total, plus synthetic column-reordering and missing-required-column cases) and a direct-API end-to-end check that uploads the real 4 fixture files through the actual `upload.js` function and confirms the response shape, plus confirms 3-file uploads still work unchanged. What did **not** get verified: an actual browser screenshot of `ItemPerformancePanel.jsx` rendering correctly with real data. `netlify dev`'s proxy (port 8888) was unreliable in the browser-automation session that day (separately, a fresh `netlify dev` couldn't even start at all once the Netlify account got suspended mid-session — see below) — confirmed the React app itself renders with no JS errors by loading it directly on Vite's port (5173), but that path can't reach the `/api/*` functions to load real report data. Worth an actual visual check next session once Netlify dev is reliably reachable again.

## Shift Retail Group logo added to the app header (2026-08-21)

- Added to `src/components/Layout.jsx` (top of every page, left of "Broker Portal" / brand name), in addition to the footer placement added earlier the same day. Same `<img>` + flexbox pattern already used for the two footer logos (Kipod AI "Powered by", Shift Retail Group "Prepared by") — no new technique introduced.
- **Not visually screenshotted** — attempted via the local `netlify dev` (offline mode) instance, but hit the same proxy corruption already logged below: network log showed `main.jsx` returning 200, yet a dynamic re-import of that exact URL immediately after failed with "Failed to fetch dynamically imported module." This is proxy-layer corruption, not a code issue — confirmed by the `npm run build` output being clean and by this being the identical `<img>`/flex pattern already live and visually correct on production (before the Netlify suspension took the production site down). Worth an actual screenshot once either `netlify dev`'s proxy stabilizes or the production site is back up.

## Incident: Netlify account suspended mid-session (2026-08-21)

- The `kipodai` Netlify account was suspended by Netlify (reason not disclosed to us beyond "contact support") partway through this session. User has already opened a support ticket. Effects observed, worst to mildest:
  1. **Production site is down** — `https://shiftretailgroup.kipodai.com` returns 404 on every route.
  2. **`netlify login` fails outright** ("Your account has been suspended").
  3. **A *linked* `netlify dev` fails to even start** — it calls the Netlify API to fetch site addon info before serving anything, and that call 401s, so local dev was fully blocked while `netlify.toml` was still linked to the suspended site.
  4. **Workaround: `netlify unlink` + `netlify dev --offline`** restores local dev — this mode never calls the Netlify API, so it works regardless of account status. Functions, Blobs emulation, and the Vite frontend all still run locally. This is what made it possible to keep building/testing this session's features (recipient list, item-level slicer) after the suspension hit.
  5. Re-linking (`netlify link`) will be needed once the account is restored, before the next real deploy.
- Per `CLAUDE.md`'s stack rule, Cloudflare Pages + Workers is the pre-approved fallback if Netlify tooling becomes unavailable for good — not invoked yet since local dev is unblocked via the offline workaround above and the user is actively working the support ticket, but worth revisiting if the suspension drags on.

## Post-Phase-5 review finding (fixed)

A third adversarial review subagent, reviewing `server/narrative.js`/`server/email.js` against the real fixtures, found a real correctness bug directly caused by the trailing-13-week correlation window decision above: **the template narrative said "This decline lines up with a drop in in-stock availability..." unconditionally whenever the `supply_driven_correlation` alert fired — even though that alert can fire from a dip several weeks in the past while the current week (L1W) has since fully recovered.** Reproduced live against the real fixtures: L1W POS $ was *up* 61.4% vs LY (the dip was weeks 202616–202619, now outside the current week), yet the generated narrative read "This week's sales were up 61.4%... **This decline** lines up with a drop in in-stock..." — directly self-contradictory, and exactly the kind of non-analyst-misleading text the REPORT SPEC's narrative rules are meant to prevent.

Fixed in `server/narrative.js`: the template now checks whether L1W itself is declining (`kpi.l1wPosDollarsChgVsLY < 0`) or the correlation alert's `weeks` array includes the current week before using "this decline" language; otherwise it describes the dip as a recent-but-resolved event ("affected sales in prior weeks... though sales have since recovered"). The Anthropic prompt was also tightened with an explicit instruction covering the same distinction, since the LLM path receives the same `weeks`/`currentWeek` fields and could make the identical mistake. Regression tests added in `tests/narrative.test.js`, including the exact real-fixture scenario (up 61.4%, alert weeks not including current week).

## Post-deploy: branding

- **`BROKER_NAME` changed from the placeholder `"Kipod AI"` to the real value `"Shift Retail Group"`.** This had been flagged as an open question in an earlier session (the admin password `shiftretail2026!` was the tell). Left unresolved, the report footer read "Prepared by Kipod AI" directly above "Powered by Kipod AI" (added when the Kipod AI hedgehog logo was added to the footer) — reading as a confusing duplicate even though the two lines mean different things. Resolved by setting `BROKER_NAME` to the actual broker name; "Powered by Kipod AI" (with logo) stays as the tool-vendor credit line, distinct from "Prepared by Shift Retail Group" (the broker whose report this is, matching the spec's footer rule for the client-facing report).
- **Kipod AI logo** lives at `logos/kipodai.png` (not under `src/assets/`) — kept in a top-level `logos/` folder since a second logo (Shift Retail Group's, not yet supplied) is expected to join it there. Imported directly via relative path from `ReportView.jsx`; Vite bundles it fine from outside `src/`.
- Deployed to production at `shiftretailgroup.kipodai.com` (custom domain, CNAME to `kipodai-broker-portal.netlify.app`) — see README deploy section. Netlify's project-level "Secure Site" (`sso_login`) had to be turned off for the app's own password auth to be reachable by non-Netlify-account visitors (it was on by default for new sites on this team/plan).

## Dual-hosting with automatic failover (2026-08-21)

- Netlify came back online after the suspension logged above. User asked for Netlify (primary) + Cloudflare (failsafe) with automatic failover. Flipped to **Cloudflare primary, Netlify backup** instead: Netlify no longer has a working backend (`netlify/functions/` is empty, no `netlify.toml`; `server/storage.js` is a Cloudflare-KV-only wrapper — see stack rule in `CLAUDE.md`), so rebuilding a parallel Netlify-Blobs backend would reintroduce the dual-maintenance risk the Cloudflare migration was meant to remove. Netlify serves as a **static-only** fallback (frontend loads, `/api/*` calls fail) if Cloudflare has an outage — accepted tradeoff, not a bug.
- `wrangler.jsonc`'s `routes`/custom-domain binding was **removed**: `kipodai.com` / `www.kipodai.com` turned out to belong to the separate KipodAI marketing site (already correctly live on Cloudflare, unrelated repo), not this broker portal — this repo's earlier `wrangler.jsonc` would have hijacked that domain on first deploy. This app's real hostname is `shiftretailgroup.kipodai.com`, which is not yet pointed at anything (DNS lookup currently empty — it was CNAME'd to the now-defunct Netlify Functions deploy before).
- Plan: a Cloudflare Load Balancer owns the `shiftretailgroup.kipodai.com` DNS record (not a Worker custom-domain binding, which can't coexist with an LB on the same hostname) — primary pool = this Worker's `*.workers.dev` hostname, backup pool = the Netlify site's `*.netlify.app` hostname, health monitor on `/` (not an API route — Netlify has no API to check, and checking `/` still lets the LB detect a real Cloudflare outage). Not yet executed — pending Netlify site hostname confirmation and Cloudflare Load Balancing API access (wrangler's OAuth token only has `zone:read`, not DNS/LB write).

## Cloudflare Pages Functions → single Worker consolidation (2026-08-21)

Earlier the same day, before discovering the dual-hosting decision above, this app's Netlify→Cloudflare migration was built against **Cloudflare Pages Functions** (`functions/api/*.js`, one file per route, file-based routing — a different Cloudflare product from a plain Worker). All the actual business logic (`server/auth.js`'s Web Crypto rewrite, `server/storage.js`'s KV rewrite, `server/parser.js`'s Buffer→Uint8Array portability fix, `server/recipients.js`'s env-threading) was written against this Pages Functions target and is unaffected by what follows — only the "front door" routing shape changed.

- **Pages Functions and a Worker-with-assets don't coexist** — they're separate Cloudflare products. Since the dual-hosting/failover plan above committed this app to a plain Worker (`wrangler.jsonc`'s `assets` config, no Pages-specific settings), the `functions/api/*.js` directory was deleted and consolidated into `worker/index.js` (a single `fetch(request, env, ctx)` entry point) + `worker/routes/*.js` (one file per route, same logic as before, just exporting a plain `(request, env) => Response` function instead of Pages' `onRequest(context)` convention). `public/_redirects` (a Pages-only SPA-fallback convention) was also removed — the Worker's `assets.not_found_handling: "single-page-application"` config in `wrangler.jsonc` covers that instead.
- **The Worker's `fetch` handler explicitly forwards non-API requests to `env.ASSETS.fetch(request)`** rather than relying on any implicit precedence between the Worker script and static-asset serving — Cloudflare's exact default-routing behavior when both a `main` script and `assets` are configured wasn't worth gambling production routing on; being explicit removes the ambiguity entirely.
- **Caught and fixed a real bug before it could ship:** `wrangler.jsonc` (already present in the repo, authored by a parallel session working the Netlify/Cloudflare failover) had `"name": "kipodai-site"` — but per that same session's own `DECISIONS.md` entry above, `kipodai-site` is the *separate marketing site's* Worker name. Cloudflare Worker names are unique per account, so deploying this repo unchanged would have overwritten the live marketing site's Worker on first `wrangler deploy`. Renamed to `shiftretailgroup-broker-portal` (matches this app's actual hostname; also fits the "one Worker per client" multi-tenant direction — see README). Also added the missing `main` (`worker/index.js`), `assets.binding` (`"ASSETS"`, referenced by `worker/index.js`), and a placeholder `kv_namespaces` entry (real namespace id still needs creating via `wrangler kv namespace create REPORTS_KV`).
- **Coordination with the parallel session building the Load Balancer/DNS side:** confirmed via cross-session message that DNS/Load Balancer config for `kipodai.com`/`shiftretailgroup.kipodai.com` stays entirely that session's scope; this session only owns the Worker code, KV binding, and `wrangler.jsonc`'s `main`/`assets`/`kv_namespaces`/`name` fields. The exact `*.workers.dev` hostname (needed as the LB's primary-pool origin) isn't known yet — deploy hasn't happened, since it needs the user's own `wrangler login` browser step first.
- Full test suite (61/61) and `npm run build` verified green after the consolidation — none of it touches the pure `server/*.js` logic the tests exercise, only how routes are dispatched.

## First live deploy to Cloudflare (2026-08-21)

- `wrangler login` initially failed/behaved oddly ("You are logged in with an API Token") because Wrangler auto-loads this repo's `.env` and treats a present `CLOUDFLARE_API_TOKEN` as an override for the stored OAuth session — and this repo's `.env` had exactly that (the parallel session's own narrow DNS/Zone/Load-Balancer-scoped token for their failover work, unrelated to Workers/KV). That token doesn't have `workers_kv:write` or deploy scopes, so it also broke `wrangler kv namespace create` with an opaque `Authentication error [code: 10000]` until the cause was found. Worked around locally by renaming `.env` out of the way for each Wrangler command that needed real OAuth permissions, then restoring it immediately after (guarded with PowerShell `try/finally` so the file is never left hidden even if a command fails). The parallel session has since renamed their variable to `FAILOVER_CLOUDFLARE_API_TOKEN` so this collision won't recur.
- **Real, reproducible bug hit during setup — password secrets set via `"value" | wrangler secret put NAME` silently stored the wrong value.** PowerShell's pipe-to-native-process mechanism appends a trailing newline (CRLF) when handing a piped string to a child process's stdin, so the *actual* stored secret was `"shiftretail2026!\r\n"`, not `"shiftretail2026!"` — every login attempt with the correct password then failed with "Incorrect password," which read at first like an auth-logic bug (`safeStringEqual`/Web Crypto rewrite) rather than an input-encoding one. Confirmed it wasn't a code bug by testing byte-for-byte: fixed by writing a small Node `child_process.spawn` wrapper (`stdio: ['pipe', ...]`, write the exact string to `child.stdin`, no trailing newline) instead of PowerShell's pipe operator — this is the reliable way to set any Wrangler secret from this environment going forward, not a one-off.
- KV namespace `REPORTS_KV` created (id `d9b77bfaf19f45769d015eae4fa2c21f`), `ADMIN_PASSWORD`/`CLIENT_PASSWORD`/`SESSION_SECRET` set as Worker secrets (same production values as the earlier Netlify deployment, so existing credentials keep working), Worker deployed via `wrangler deploy`.
- **Live at `https://shiftretailgroup-broker-portal.shai-0d2.workers.dev`.** Verified end-to-end, not just "it deployed": a 14-point smoke test (wrong-password rejection, no-cookie rejection, admin/client login, session role reporting, 403 on admin-only routes for a client cookie, `email-config`'s `not_configured` state with no `RESEND_API_KEY` set, empty-KV `list-reports`, 404 on no-published-report, `send-email`'s graceful `not_configured` response, and both the real `/` SPA route and a client-side-only route like `/archive` correctly falling through to `env.ASSETS.fetch()`) — all passing. Then a full real-fixture cycle: uploaded all 4 real fixture files (including the item-level Sales_Performance file) through `/api/upload?mode=publish`, confirmed `itemPerformance` data came through, retrieved it via `get-report`, confirmed it in `list-reports`, then deleted it via `delete-report` to leave no stray state in the fresh KV namespace.
- Reported the live hostname back to the parallel session (cross-session message) as the Load Balancer's primary-pool origin — DNS/LB wiring itself remains entirely their scope, not touched here.

## DNS failover via GitHub Actions, not Cloudflare Load Balancer (2026-08-21)

- Cloudflare's Load Balancer product needs a paid add-on enabled on the `kipodai.com` zone (currently on the free plan) — a real `POST .../load_balancers/pools` call against it returned a generic `1002 validation failed` error, traced to Load Balancing simply not being enabled yet (confirmed via the zone's Traffic → Load Balancing dashboard page, which shows an onboarding screen with an "Enable Load Balancing" button rather than any existing config). User asked to find a free alternative rather than pay for it.
- **Built instead: a scheduled GitHub Actions workflow (`.github/workflows/dns-failover.yml`, `scripts/dns-failover.mjs`) that does the same job for free.** Every 15 minutes it health-checks the Cloudflare origin directly; if unhealthy, it checks the Netlify origin and — if that's healthy — flips `shiftretailgroup.kipodai.com`'s CNAME to Netlify via the Cloudflare DNS API (the same scoped token from the Load Balancer attempt already has `DNS:Edit`, so no new credential was needed). It flips back to Cloudflare automatically once the primary origin recovers. Both origins unhealthy → DNS is left untouched and the run fails loudly (free alerting via GitHub's own workflow-failure notifications).
- 15-minute interval chosen deliberately, not the more responsive 5-minute one: `kipodai/kipodai-broker-portal` is a private repo, so scheduled Actions minutes count against the account's included free minutes (2,000/month on GitHub Free). 5-minute checks would burn through that budget on this workflow alone; 15-minute checks land comfortably under it with room for other CI usage. Tradeoff: failover detection is bounded by ~15 minutes plus DNS TTL propagation, not the near-instant response Cloudflare's own Load Balancer would give — accepted given the "free" constraint.
- **Known blind spot, same one Cloudflare's own Load Balancer would have:** this only protects against the Worker/app itself failing (bad deploy, KV issue, account suspension) — not a full Cloudflare edge outage, since DNS for this zone is itself hosted on Cloudflare. A true full-Cloudflare-outage failover would need DNS hosted on a different provider entirely, which is out of scope here.
- **Not yet wired up — two placeholders remain**, both because the Worker hasn't been deployed yet (see the consolidation entry above): the GitHub repo variables `CF_ORIGIN_HEALTH_URL` and `CF_CNAME_TARGET` need the real `shiftretailgroup-broker-portal.<workers.dev-subdomain>` hostname once the other session deploys. Until then this workflow will keep flipping (or leaving) DNS toward Netlify only, since the Cloudflare health check has nothing real to hit. `CLOUDFLARE_ZONE_ID` (`869c16bf3ed4c029e72015f1490ed793`) and the `CLOUDFLARE_API_TOKEN` secret also still need adding to the repo's Actions settings — not done yet, pending user confirmation before writing a standing credential into GitHub.
- **Netlify itself is currently unreachable — 401 from `kipodai-broker-portal.netlify.app` on every request**, `Server: Netlify` header confirmed it's Netlify's own edge, not the app. Traced to a brand-new "Private by default" project-visibility feature (the Netlify account is one day old — recreated after the suspension) shipping `Production visibility: Password`. Could not fix it: every dashboard path to the actual visibility control (General → "Edit visibility", Access & security → "Go to visitor access", the pre-rebrand `/sites/.../settings/access` URL, the "Private by default" sidebar coachmark) redirects to an unrelated page instead of opening the control — reproduced 4 separate ways, so this is a real Netlify dashboard bug, not user error. The legacy API workaround (`PATCH /api/v1/sites/{id}` with `password: null`/`""`) didn't work either — `has_password`/`password_context` are read-only reflections of the new system, not the old field. User decided to leave this for now rather than keep digging (e.g. Netlify support). Net effect: the failover workflow above will correctly treat Netlify as unhealthy and refuse to route to it until this is fixed — safe, just means there's effectively no working backup yet even once the Cloudflare side is wired up.
